services:
  db:
    image: postgres:12
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust # Unsecure, but fine for just for running tests.
  binder:
    build: .
    command: tail -f /dev/null
    volumes:
      - .:/binder
    depends_on:
      - db
